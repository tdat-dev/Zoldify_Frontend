name: 🔍 CI Pipeline

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

env:
  PHP_VERSION: "8.2"
  NODE_VERSION: "18"

jobs:
  # ============================================
  # JOB 1: PHP Lint + Code Quality
  # ============================================
  php-quality:
    name: 🐘 PHP Quality Check
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          tools: composer:v2
          coverage: xdebug

      - name: 📦 Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      - name: ✅ Validate composer.json
        run: composer validate --no-interaction

      - name: 📥 Install dependencies
        run: composer install --no-interaction --prefer-dist --no-progress

      - name: 🔍 Determine changed PHP files
        id: changed
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE="${{ github.event.pull_request.base.sha }}"
            HEAD="${{ github.event.pull_request.head.sha }}"
          else
            BASE="${{ github.event.before }}"
            HEAD="${{ github.sha }}"
          fi

          echo "Base: $BASE"
          echo "Head: $HEAD"

          {
            echo "files<<EOF"
            if [[ "$BASE" == "0000000000000000000000000000000000000000" ]]; then
              git diff-tree --no-commit-id --name-only -r "$HEAD" -- '*.php' ':!vendor/**' || true
            else
              git diff --name-only "$BASE" "$HEAD" -- '*.php' ':!vendor/**' || true
            fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: 🧪 PHP Syntax Check
        shell: bash
        run: |
          set -euo pipefail
          FILES="${{ steps.changed.outputs.files }}"

          if [[ -z "$FILES" ]]; then
            echo "✅ No PHP files changed. Skip syntax check."
            exit 0
          fi

          echo "🔍 Checking syntax for changed files..."
          while IFS= read -r f; do
            [[ -z "$f" ]] && continue
            if [[ -f "$f" ]]; then
              php -l "$f"
            else
              echo "⏭️ Skipping deleted file: $f"
            fi
          done <<< "$FILES"

      - name: 🔒 Security Audit (Composer)
        run: composer audit --no-interaction
        continue-on-error: true

  # ============================================
  # JOB 2: Node.js Check (Chat Server)
  # ============================================
  node-quality:
    name: 🟢 Node.js Quality Check
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: chat-server/package-lock.json

      - name: 📥 Install dependencies
        working-directory: ./chat-server
        run: npm ci

      - name: 🧪 Check for syntax errors
        working-directory: ./chat-server
        run: node --check index.js

      - name: 🔒 Security Audit (npm)
        working-directory: ./chat-server
        run: npm audit --audit-level=high
        continue-on-error: true

  # ============================================
  # NOTE: CodeQL Security Scan removed
  # Reason: Requires GitHub Advanced Security (paid) for private repos
  # Security is still checked via:
  #   - composer audit (in php-quality job)
  #   - npm audit (in node-quality job)
  # ============================================

  # ============================================
  # JOB 4: Build Verification
  # ============================================
  build-check:
    name: 🏗️ Build Verification
    runs-on: ubuntu-latest
    needs: [php-quality, node-quality]

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          tools: composer:v2

      - name: 📦 Cache Composer
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}

      - name: 📥 Install PHP dependencies
        run: composer install --no-interaction --prefer-dist --no-progress --optimize-autoloader

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📥 Install Node dependencies
        working-directory: ./chat-server
        run: npm ci --production

      - name: ✅ Verify project structure
        run: |
          echo "🔍 Checking critical files..."
          test -f "public/index.php" && echo "✅ public/index.php exists"
          test -f "composer.json" && echo "✅ composer.json exists"
          test -f "chat-server/index.js" && echo "✅ chat-server/index.js exists"
          test -d "app/Controllers" && echo "✅ app/Controllers/ exists"
          test -d "app/Models" && echo "✅ app/Models/ exists"
          echo "🎉 All critical files verified!"

  # ============================================
  # Summary
  # ============================================
  ci-summary:
    name: 📋 CI Summary
    runs-on: ubuntu-latest
    needs: [php-quality, node-quality, build-check]
    if: always()

    steps:
      - name: 📊 Check job results
        run: |
          echo "========================================"
          echo "           CI PIPELINE SUMMARY          "
          echo "========================================"
          echo "PHP Quality:     ${{ needs.php-quality.result }}"
          echo "Node.js Quality: ${{ needs.node-quality.result }}"
          echo "Build Check:     ${{ needs.build-check.result }}"
          echo "========================================"
          echo ""
          echo "📝 Note: Security checks are included in"
          echo "   PHP Quality (composer audit) and"
          echo "   Node.js Quality (npm audit) jobs."
          echo "========================================"

          if [[ "${{ needs.php-quality.result }}" == "failure" ]] || \
             [[ "${{ needs.node-quality.result }}" == "failure" ]] || \
             [[ "${{ needs.build-check.result }}" == "failure" ]]; then
            echo "❌ CI Pipeline FAILED!"
            exit 1
          else
            echo "✅ CI Pipeline PASSED!"
          fi
