# ============================================
# ZOLDIFY CHAT SERVER - Environment Config
# ============================================
# Copy file này thành .env
# cp .env.example .env
# ============================================

# ================== SERVER CONFIG ==================
SOCKET_PORT=3001                  # Port cho Socket.IO server
CORS_ORIGIN=http://localhost:8000,https://zoldify.com   # Danh sách domain được phép (phân cách bằng dấu phẩy)

# ================== DATABASE (MySQL) ==================
# Phải giống với config PHP để đọc/ghi messages
DB_HOST=localhost
DB_USER=root
DB_PASS=                          # Password MySQL
DB_NAME=zoldify                   # Tên database (giống PHP)

# ================== REDIS (Optional) ==================
# Dùng cho Socket.IO Adapter khi scale nhiều server
# Nếu không có, chạy standalone mode
REDIS_URL=                        # Ví dụ: redis://localhost:6379
